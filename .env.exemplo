# ================================================
# AGENTE DE LICITAÇÕES MS — Variáveis de Ambiente
# ================================================
# No Railway: configure estas variáveis em
# Settings → Variables (não use este arquivo lá)

# Chave da API Anthropic — obtenha em console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-COLOQUE_SUA_CHAVE_AQUI

# Configurações do PCA (órgão padrão)
ORGAO_CNPJ=03015475000140
PCA_ANO=2026
PCA_SEQ=3

# Porta (o Railway define automaticamente — não altere)
PORT=5000
